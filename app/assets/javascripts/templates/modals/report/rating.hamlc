%button.rate.down{disabled: !@model.can_vote, class: (if @model.can_vote then '' else 'disabled'), 'data-key': 'negatives'}
#current_rating= @model.rating
%button.rate.up{disabled: !@model.can_vote, class: (if @model.can_vote then '' else 'disabled'), 'data-key': 'positives'}
